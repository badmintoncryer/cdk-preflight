id: pf-aoss-network-policy-allow-from-public-required
service: aoss
benchRegion: us-east-1
resourceTypes: [AWS::OpenSearchServerless::SecurityPolicy]
severity: ERROR
title: "Every network policy block needs AllowFromPublic"
constraintSource: https://docs.aws.amazon.com/opensearch-service/latest/developerguide/serverless-network.html
upstream: none
repro:
  method: real-deploy
  evidence: "bench 2026-09-25 us-east-1: fail (network policy block with Rules but no AllowFromPublic) -> \"Policy json is invalid, error: [$[0].SourceVPCEs: is missing but it is required, $[0].AllowFromPublic: is missing but it is required, $[0].SourceServices: is missing but it is required, $[0]: should have a minimum of 2 properties]\" (ROLLBACK_COMPLETE). pass not deployed (--fail-only: the policy resources are free, but the run is kept to the fail side; api 2026-09-25 us-east-1 deployed the matching control through CreateAccessPolicy/CreateSecurityPolicy/CreateLifecyclePolicy and it was ACCEPTED)"
addedOn: 2026-09-25

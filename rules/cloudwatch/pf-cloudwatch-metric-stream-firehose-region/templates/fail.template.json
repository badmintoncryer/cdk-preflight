{
  "Resources": {
    "Stream": {
      "Type": "AWS::CloudWatch::MetricStream",
      "Properties": {
        "FirehoseArn": {
          "Fn::Sub": "arn:${AWS::Partition}:firehose:eu-west-1:${AWS::AccountId}:deliverystream/pf-stream"
        },
        "RoleArn": {
          "Fn::Sub": "arn:${AWS::Partition}:iam::${AWS::AccountId}:role/pf-metric-stream"
        },
        "OutputFormat": "json"
      }
    }
  }
}
